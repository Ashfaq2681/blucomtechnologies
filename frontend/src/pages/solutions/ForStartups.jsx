import SolutionPage from "./SolutionPage";
import { solutionsContent } from "./solutionsContent";

export default function ForStartups() {
  return <SolutionPage content={solutionsContent.startups} />;
}
