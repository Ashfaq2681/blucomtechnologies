import SolutionPage from "./SolutionPage";
import { solutionsContent } from "./solutionsContent";

export default function ForAgencies() {
  return <SolutionPage content={solutionsContent.agencies} />;
}
