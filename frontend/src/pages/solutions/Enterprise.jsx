import SolutionPage from "./SolutionPage";
import { solutionsContent } from "./solutionsContent";

export default function Enterprise() {
  return <SolutionPage content={solutionsContent.enterprise} />;
}
